#ifndef __NFUNCTIONS_H__
#define __NFUNCTIONS_H__

#include "netlib.h"

#include <string>

using namespace std;

string host_to_address(string host);
string address_to_host(string address);

#endif
